# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Optional - can be configured via UI)
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=asset_manager
DB_USER=your_username
DB_PASSWORD=your_password
DB_SSL=false